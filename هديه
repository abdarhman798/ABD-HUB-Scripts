Tab("🛡️ الحقوق", 3, function() 
    local pprof = Instance.new("Frame", Content); pprof.Size = UDim2.new(1, 0, 0, 160); pprof.BackgroundColor3 = Color3.new(ThemeColor.R*0.12, ThemeColor.G*0.12, ThemeColor.B*0.12); pprof.BackgroundTransparency = 0.35; pprof.ZIndex = 4; Instance.new("UICorner", pprof); table.insert(ThemedMenus, pprof)
    

    local devImg = Instance.new("ImageLabel", pprof); devImg.Size = UDim2.new(0, 60, 0, 60); devImg.Position = UDim2.new(0.25, -30, 0, 15); devImg.BackgroundTransparency = 1; devImg.ZIndex = 4; Instance.new("UICorner", devImg).CornerRadius = UDim.new(1, 0)
    local devStroke = Instance.new("UIStroke", devImg); devStroke.Thickness = 2.5; devStroke.Color = Color3.fromRGB(255, 255, 255)
    local devGrad = Instance.new("UIGradient", devStroke); devGrad.Color = ColorSequence.new({ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 0, 0)), ColorSequenceKeypoint.new(0.4, Color3.fromRGB(255, 0, 0)), ColorSequenceKeypoint.new(0.5, Color3.fromRGB(255, 255, 255)), ColorSequenceKeypoint.new(0.6, Color3.fromRGB(255, 0, 0)), ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 0, 0))})
    
    local title = Instance.new("TextLabel", pprof); title.Size = UDim2.new(0.5, 0, 0, 20); title.Position = UDim2.new(0, 0, 0, 85); title.BackgroundTransparency = 1; title.Text = GetTr("عبود المطور"); title.TextColor3 = Color3.new(1, 1, 1); title.Font = SafeFont; title.TextSize = 14; title.ZIndex = 4
    local nameLabel = Instance.new("TextLabel", pprof); nameLabel.Size = UDim2.new(0.5, 0, 0, 20); nameLabel.Position = UDim2.new(0, 0, 0, 105); nameLabel.BackgroundTransparency = 1; nameLabel.Text = "@ABD777HH"; nameLabel.TextColor3 = Color3.new(0.7, 0.7, 0.7); nameLabel.Font = SafeFont; nameLabel.TextSize = 12; nameLabel.ZIndex = 4


    local adminImg = Instance.new("ImageLabel", pprof); adminImg.Size = UDim2.new(0, 60, 0, 60); adminImg.Position = UDim2.new(0.75, -30, 0, 15); adminImg.BackgroundTransparency = 1; adminImg.ZIndex = 4; Instance.new("UICorner", adminImg).CornerRadius = UDim.new(1, 0)
    local adminStroke = Instance.new("UIStroke", adminImg); adminStroke.Thickness = 2.5; adminStroke.Color = Color3.fromRGB(255, 255, 255)
    local adminGrad = Instance.new("UIGradient", adminStroke); adminGrad.Color = ColorSequence.new({ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 0, 0)), ColorSequenceKeypoint.new(0.4, Color3.fromRGB(255, 0, 0)), ColorSequenceKeypoint.new(0.5, Color3.fromRGB(255, 255, 255)), ColorSequenceKeypoint.new(0.6, Color3.fromRGB(255, 0, 0)), ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 0, 0))})
    
    local adminTitle = Instance.new("TextLabel", pprof); adminTitle.Size = UDim2.new(0.5, 0, 0, 20); adminTitle.Position = UDim2.new(0.5, 0, 0, 85); adminTitle.BackgroundTransparency = 1; adminTitle.Text = GetTr("اداري سكربت"); adminTitle.TextColor3 = Color3.new(1, 1, 1); adminTitle.Font = SafeFont; adminTitle.TextSize = 14; adminTitle.ZIndex = 4
    local adminNameLabel = Instance.new("TextLabel", pprof); adminNameLabel.Size = UDim2.new(0.5, 0, 0, 20); adminNameLabel.Position = UDim2.new(0.5, 0, 0, 105); adminNameLabel.BackgroundTransparency = 1; adminNameLabel.Text = "@Dhoomsale"; adminNameLabel.TextColor3 = Color3.new(0.7, 0.7, 0.7); adminNameLabel.Font = SafeFont; adminNameLabel.TextSize = 12; adminNameLabel.ZIndex = 4


    task.spawn(function()
        pcall(function() devImg.Image = "rbxthumb://type=AvatarHeadShot&id="..game:GetService("Players"):GetUserIdFromNameAsync("ABD777HH").."&w=150&h=150" end)
        pcall(function() adminImg.Image = "rbxthumb://type=AvatarHeadShot&id="..game:GetService("Players"):GetUserIdFromNameAsync("Dhoomsale").."&w=150&h=150" end)
        
        local rot = 0
        while task.wait(0.02) do
            rot = (rot + 3) % 360
            devGrad.Rotation = rot
            adminGrad.Rotation = rot
        end
    end)

    
    _G.AboudLeaderboard = _G.AboudLeaderboard or {}

    local lbContainer = Instance.new("Frame", Content)
    lbContainer.Size = UDim2.new(1, 0, 0, 220)
    lbContainer.BackgroundColor3 = Color3.fromRGB(30, 8, 12) 
    lbContainer.BackgroundTransparency = 0.1
    lbContainer.ZIndex = 4
    Instance.new("UICorner", lbContainer).CornerRadius = UDim.new(0, 8)
    
    local lbStroke = Instance.new("UIStroke", lbContainer)
    lbStroke.Color = Color3.fromRGB(150, 20, 
    lbStroke.Thickness = 1.5

    local lbTitle = Instance.new("TextLabel", lbContainer)
    lbTitle.Size = UDim2.new(1, 0, 0, 30)
    lbTitle.Position = UDim2.new(0, 0, 0, 5)
    lbTitle.BackgroundTransparency = 1
    lbTitle.Text = "Top 20 - اعلى مدة بقاء"
    lbTitle.TextColor3 = Color3.fromRGB(255, 100, 100)
    lbTitle.Font = Enum.Font.GothamBold
    lbTitle.TextSize = 14
    lbTitle.ZIndex = 5

    local lbScroll = Instance.new("ScrollingFrame", lbContainer)
    lbScroll.Size = UDim2.new(1, -10, 1, -45)
    lbScroll.Position = UDim2.new(0, 5, 0, 40)
    lbScroll.BackgroundTransparency = 1
    lbScroll.ScrollBarThickness = 4
    lbScroll.ClipsDescendants = true
    lbScroll.ScrollBarImageColor3 = Color3.fromRGB(180, 30, 40)
    lbScroll.ZIndex = 5
    
    local lbLayout = Instance.new("UIListLayout", lbScroll)
    lbLayout.Padding = UDim.new(0, 5)
    lbLayout.SortOrder = Enum.SortOrder.LayoutOrder


    lbLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
        lbScroll.CanvasSize = UDim2.new(0, 0, 0, lbLayout.AbsoluteContentSize.Y + 5)
    end)


    local UI_Slots = {}
    for i = 1, 20 do
        local slot = Instance.new("Frame", lbScroll)
        slot.Size = UDim2.new(1, 0, 0, 45)
        slot.BackgroundColor3 = Color3.fromRGB(45, 12, 18)
        slot.ZIndex = 6
        slot.Visible = false
        Instance.new("UICorner", slot).CornerRadius = UDim.new(0, 6)
        
        local rankLbl = Instance.new("TextLabel", slot)
        rankLbl.Size = UDim2.new(0, 30, 1, 0)
        rankLbl.Position = UDim2.new(0, 5, 0, 0)
        rankLbl.BackgroundTransparency = 1
        rankLbl.TextColor3 = Color3.new(1, 1, 1)
        rankLbl.Font = Enum.Font.GothamBold
        rankLbl.TextSize = 14
        rankLbl.ZIndex = 7

        local img = Instance.new("ImageLabel", slot)
        img.Size = UDim2.new(0, 35, 0, 35)
        img.Position = UDim2.new(0, 40, 0, 5)
        img.BackgroundTransparency = 1
        img.ZIndex = 7
        Instance.new("UICorner", img).CornerRadius = UDim.new(1, 0)

        local nameLbl = Instance.new("TextLabel", slot)
        nameLbl.Size = UDim2.new(1, -160, 0, 20)
        nameLbl.Position = UDim2.new(0, 85, 0, 5)
        nameLbl.BackgroundTransparency = 1
        nameLbl.TextColor3 = Color3.new(1, 1, 1)
        nameLbl.TextXAlignment = Enum.TextXAlignment.Left
        nameLbl.Font = Enum.Font.GothamBold
        nameLbl.TextSize = 13
        nameLbl.ZIndex = 7

        local timeLbl = Instance.new("TextLabel", slot)
        timeLbl.Size = UDim2.new(1, -160, 0, 15)
        timeLbl.Position = UDim2.new(0, 85, 0, 25)
        timeLbl.BackgroundTransparency = 1
        timeLbl.TextColor3 = Color3.fromRGB(200, 200, 200)
        timeLbl.TextXAlignment = Enum.TextXAlignment.Left
        timeLbl.Font = SafeFont
        timeLbl.TextSize = 11
        timeLbl.ZIndex = 7
        
        UI_Slots[i] = {Frame = slot, Rank = rankLbl, Img = img, Name = nameLbl, Time = timeLbl}
    end

    local function RefreshLeaderboard()
        local sortedPlayers = {}
        for name, data in pairs(_G.AboudLeaderboard) do
            table.insert(sortedPlayers, {Name = name, UserId = data.UserId, Time = data.Time})
        end
        
        table.sort(sortedPlayers, function(a, b) return a.Time > b.Time end)

        if #sortedPlayers == 0 then
            UI_Slots[1].Frame.Visible = true
            UI_Slots[1].Rank.Text = "#1"
            UI_Slots[1].Name.Text = "لا يوجد"
            UI_Slots[1].Time.Text = "00:00:00"
            UI_Slots[1].Img.Image = "rbxassetid://10111280387" 
            
            for i = 2, 20 do UI_Slots[i].Frame.Visible = false end
        else
            for i = 1, 20 do
                if sortedPlayers[i] then
                    local p = sortedPlayers[i]
                    UI_Slots[i].Frame.Visible = true
                    UI_Slots[i].Rank.Text = "#" .. i
                    UI_Slots[i].Name.Text = p.Name
                    
                    local h = math.floor(p.Time / 3600)
                    local m = math.floor((p.Time % 3600) / 60)
                    local s = math.floor(p.Time % 60)
                    UI_Slots[i].Time.Text = string.format("%02d:%02d:%02d", h, m, s)
                    
                    UI_Slots[i].Img.Image = "rbxthumb://type=AvatarHeadShot&id="..p.UserId.."&w=150&h=150"
                    
                    if i == 1 then
                        UI_Slots[i].Rank.TextColor3 = Color3.fromRGB(255, 215, 0)
                        UI_Slots[i].Name.TextColor3 = Color3.fromRGB(255, 215, 0)
                    else
                        UI_Slots[i].Rank.TextColor3 = Color3.new(1, 1, 1)
                        UI_Slots[i].Name.TextColor3 = Color3.new(1, 1, 1)
                    end
                else
                    UI_Slots[i].Frame.Visible = false
                end
            end
        end
    end
